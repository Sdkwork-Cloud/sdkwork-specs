version: 1
defaultProfile: cloud.production

profiles:
  cloud.production:
    install:
      layout: binary-package
    expose:
      - domain: im.sdkwork.com
        tls: sdkwork.com
        mode: web+api
        web: adaptive
    packages:
      - flutter-mobile
      - harmony-mobile
      - mini-program-weixin

  standalone.development:
    expose:
      - domain: localhost
        tls: off
        mode: web+api
        web: adaptive
    packages: []
