# static-fallback when neither PC nor H5 SPA is installed (SDKWORK_DEPLOY_SPEC.md §8).
# Ordinary static file serving — do not invent a missing SPA shell.
root /usr/share/sdkwork/webserver/web/static;
index index.html;
try_files $uri $uri/ =404;
